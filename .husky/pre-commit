#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to check and fix staged files
npx lint-staged

# Run tests before commit
npm run test:run

# Run type checking
npm run type-check